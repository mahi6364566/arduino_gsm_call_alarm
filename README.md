# arduino_gsm_call_alarm
GSM call controller using Arduino &amp; SIM800L. Supports incoming/outgoing calls, auto-answer after 3 seconds, and detects call states (answered, busy, not answered, ended). Uses non-blocking logic and controls a buzzer on pin 12 based on call status. Ideal for IoT, alert, and automation systems.
